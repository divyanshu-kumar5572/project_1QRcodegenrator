# QR Code Generator
A simple Python utility to generate QR codes from URLs or text.

## Requirements
- Python 3.x
- "qrcode" library ("pip install qrcode[pil]")

## Usage
Run "makeQr.py" and enter the text you want to encode.
